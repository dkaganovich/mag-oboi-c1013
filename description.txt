Mapper. 
Разбивает входные строки по символу табуляции на тройки (src, tgt, weight). Генерирует пары вида (tgt, weight).
Reducer.
Для каждой вершины tgt суммирует значения весов weight всех входящих дуг.

